<!--   <ul class="nav nav-pills">
    <% if user_signed_in? %>
      <li class="<%= nav_active_class(:photo) %>"><%= link_to 'Photos', photos_path %></li>
      <li class="<%= nav_active_class(:conversation) %>"><%= link_to 'Conversations', conversations_path %></li>
      <li class="<%= nav_active_class(:photo) %>"><%= link_to 'Upload', new_photo_path %></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to 'Settings', edit_user_registration_path %></li>
          <% if current_user.is_admin? %>
            <li><%= link_to 'Admin', admin_photos_path %></li>
          <% end %>
          <li role="separator" class="divider"></li>
          <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
        </ul>
      </li>
    <% else %>
      <li><%= link_to 'Login', new_user_session_path %></li>
    <% end %>
  </ul>
  
 -->

 <nav>

 <div class="navigation-bar">
  <div class="logo">
  </div>

  
   <% if user_signed_in? %>
     <%= link_to 'Photos', photos_path %>
     <%= link_to 'Conversations', conversations_path %>
     <%= link_to 'Leaderboard', leaderboard_path %>
     <%= link_to 'Upload', new_photo_path %>
     <%= link_to 'Settings', edit_user_registration_path %>
     <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
     <% if current_user.is_admin? %>
       <%= link_to 'Admin', admin_photos_path %>
     <% end %>
   <% else %>
    <%= link_to 'Login', new_user_session_path %>
    <%= link_to 'Register', new_user_registration_path %>
  <% end %>
  </div>
 </nav>